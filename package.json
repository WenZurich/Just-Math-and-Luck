{
  "name": "stock-picks-site",
  "private": true,
  "version": "1.0.0",
  "type": "module",
  "scripts": {
    "dev": "vite",
    "build": "vite build",
    "preview": "vite preview",
    "validate": "node scripts/validate-data.mjs",
    "paper": "node scripts/paper-trade.mjs",
    "fetch-social": "node scripts/fetch-social.mjs --latest public/data/latest.json --out public/data/social-digest.json"
  },
  "devDependencies": {
    "vite": "^6.2.0"
  }
}
